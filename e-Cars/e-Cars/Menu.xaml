<UserControl x:Class="e_Cars.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Style="{StaticResource UserControlStyle}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Button Name="Button_Stammdatenverwalten" Margin="5" Click="Button_Stammdatenverwalten_Click" Content="_Stammdatenverwalten" />
            </Grid>

            <Grid Grid.Column="1">
                <Button Name="Button_Reservierung" Margin="5" Click="Button_Reservierung_Click" Content="Reservierung" />
            </Grid>

            <Grid Grid.Column="2">
                <Button Margin="5" Click="Button_Click_1" Content="Platzhalter ..." /> 
            </Grid>
        </Grid>
    </Grid>
</UserControl>
